import 'package:flutter/material.dart';

class NewContentTagsScreen extends StatelessWidget {
  static const routeName = '/new-tags';
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}